//var index = require('./index');
var tokenCreation = require('../services/tokenCreation');
module.exports = function(app,express){
   
    var api = express.Router();

    api.post('/',);
   // api.get('/user/:email',index.user.profileInfo);

    return api;
};
